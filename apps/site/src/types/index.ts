/**
 * Ré-export des types domaine partagés (@lignezero/types), source unique
 * commune au vitrine et à l'esport manager. Les imports historiques
 * `@/types` continuent de fonctionner tels quels.
 */
export * from '@lignezero/types';
